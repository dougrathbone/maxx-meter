{
  "name": "maxx-meter",
  "version": "0.1.1",
  "description": "Token maxxing usage dashboard for Sonoff NSPanel — Cursor, Claude, Kimi",
  "type": "module",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc && npm run build:dashboard",
    "build:dashboard": "npm run build --prefix dashboard",
    "dev": "tsx watch src/index.ts",
    "start": "node dist/index.js",
    "test": "vitest run",
    "test:watch": "vitest",
    "version:sync": "node scripts/sync-version.mjs",
    "version:check": "node scripts/check-version.mjs"
  },
  "engines": {
    "node": ">=20"
  },
  "license": "MIT",
  "dependencies": {
    "@fastify/static": "^10.1.3",
    "fastify": "^5.2.1",
    "mqtt": "^5.10.4",
    "zod": "^4.4.3"
  },
  "devDependencies": {
    "@types/node": "^26.3.0",
    "tsx": "^4.19.3",
    "typescript": "^7.0.2",
    "vitest": "^4.1.11"
  }
}
